# PART-14 — بوابة التكامل والتشغيل والتسليم

الحالة: **LOCAL_VERIFIED** — اكتملت بوابة التسليم المحلية في 2026-09-21. لا تعني هذه الحالة جاهزية إنتاجية أو إغلاق PART-02.

## ما نُفذ

- أضيف `test:e2e` للرحلات الحرجة على PostgreSQL فعلية، وجرى بنجاح 9/9.
- أضيف `demo:stories` وشُغلت القصص الخيرية والاستثمارية والبرامج والتوظيف والتمكين كاملة؛ أصلح shim Windows فشل `uv_os_get_passwd ENOMEM` في scripts.
- أضيف `demo:reset` مع guards للبيئة وloopback واسم قاعدة demo/test.
- CI يشغل integration ثم critical E2E ثم seed stories؛ لا نشر عند فشل gate.
- أضيف restore rehearsal فعلي بقاعدتي backup/restore معزولتين. النتيجة: 55 users، 4 organizations، 5 projects، 4 contributions، 44 ledger entries، صفر transaction غير متوازنة، وصفر مرجع document يتيم؛ الزمن ثانية وRPO داخل اللقطة صفر.
- جرى تدقيق سجل الأفعال وتصحيح حالات قديمة: PER-01.A03 وORG-07.A02 وPER-09.A03 وPUB-09.A02/A03 منفذة فعلًا.
- أضيف runbook وبوابات إطلاق وفجوات التكاملات الخارجية.

## نتائج بوابة الإغلاق

- `pnpm build`: نجح.
- `pnpm lint`: نجح.
- `pnpm typecheck`: نجح.
- `pnpm test`: نجح 72/72.
- `pnpm test:integration`: نجح 18/18 على PostgreSQL فعلية.
- `pnpm test:e2e`: نجح 9/9 للرحلات الحرجة.
- `pnpm demo:stories`: نجح لكل قصص العرض، مع بقاء الخطوات البشرية المقصودة معلنة.
- `pnpm ops:restore-rehearsal`: نجح؛ طابقت قاعدة الاستعادة المصدر في العدادات الحرجة، ولم يظهر قيد دفتر غير متوازن أو مرجع مستند يتيم.

## بوابة الواجهة

- `/ar/impact` فُحصت على desktop وعلى `390×844`: RTL سليم، ولا overflow (`scrollWidth <= clientWidth`)، ولا console warnings/errors.
- كشف الفحص تعريف مساهمات إنجليزيًا داخل العربية؛ صُحح إلى «مساهمات مؤكدة» وتعريف عربي يصرح بأن الدفع محاكى.
- القياس النهائي: `innerWidth=390` و`clientWidth=375` و`scrollWidth=375` مع `dir=rtl`، ومن دون تحذيرات أو أخطاء console.

## فجوات التكامل والتسليم المعلنة

- بقيت 12 عملية موسومة صراحة بأنها غير متاحة في سجل الأفعال؛ لا يوجد زر صامت لها. تشمل إغلاق الحساب، تحديد الموقع، اتفاقات الشراكة، أرشفة المشروع، مرفقات التقارير، إعادة محاولة دفع يدوية، منح كشف هوية مساهم، إعادة webhook، مقارنة العروض، رفع السيرة، ومواد التدريب.
- البريد والدفع وفحص البرمجيات الخبيثة والتخزين الكائني تكاملات محلية/محاكاة؛ مفاتيح الإنتاج وعمليات الإطلاق تبقى بوابات خارجية.
- استعادة الإنتاج يجب أن تعيد هذا التمرين باستخدام نسخة مشفرة وPITR وبنية خارج الجهاز، ثم تسجل RPO/RTO فعليين.

## حدود صريحة

- Docker و`pg_dump` غير متاحين على هذا الجهاز؛ استخدم rehearsal نسخة PostgreSQL template محلية، وليس ادعاء PITR/نسخ مشفر إنتاجي.
- قارئ شاشة لم يُشغّل بعد، فلا ادعاء تحقق WCAG كامل.
- PART-02 ما زالت تحتاج إغلاق بوابتها المحمية المستقلة؛ PART-14 لا تزيف ذلك إلى نجاح شامل.
